#include<stdio.h>
int main(){
     int a ;
     scanf("%d",&a);
     if (a==+0)
     {
      printf("A is +");
     }
     
    else if (a== -a)
    {
        printf("A neg");
    }
    else if (a == 0)
    {
       printf("a is zero");

    }
    else
    printf("invALID ");
    return 0;
}